import Connection from '../../index';
import { InfoMessage } from '../types';
declare const onErrorMessageAsync: (connection: Connection) => () => Promise<void | InfoMessage>;
export default onErrorMessageAsync;
