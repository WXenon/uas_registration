import Connection from '../../index';
import { InfoMessage } from '../types';
declare const onInfoMessageAsync: (connection: Connection) => () => Promise<void | InfoMessage>;
export default onInfoMessageAsync;
